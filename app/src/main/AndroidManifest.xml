<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.olingasoftware.projectapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/image_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/image_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectApp">
        <activity android:name=".WIthdrawGroupActivity" />
        <activity android:name=".IncomeGroupActivity" />
        <activity android:name=".WithdrawMemActivity" />
        <activity android:name=".DepositMemActivity" />
        <activity android:name=".ManageAccountActivity" />
        <activity android:name=".JoinNewGroupActivity" />
        <activity android:name=".SelctGroupActivity" />
        <activity android:name=".NewGroupActivity" />
        <activity android:name=".NewAccountActivity" />
        <activity android:name=".JoinGroupInterfaceActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".LoanActivity" />
        <activity android:name=".TransactionsActivity" />
        <activity android:name=".AccountsActivity" />
        <activity android:name=".MyAccountctivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".LoginActivity" /> <!-- <activity -->
        <!-- android:name=".loan.LoanActivity" -->
        <!-- android:label="@string/title_activity_loan" /> -->
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainHomeActivity"
            android:label="@string/title_activity_main_home" />
    </application>

</manifest>